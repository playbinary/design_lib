#include "wlist.h"
#include <malloc.h>

int main (void){
    uint32_t * eactiveRam = (uint32_t *)malloc(EACTIVERAM_DEEPTH * 4);
    uint32_t * dactiveRam = (uint32_t *)malloc(DACTIVERAM_DEEPTH * 4);



}